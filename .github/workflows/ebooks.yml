name: Build Ebooks
on: 
  push:
    paths: ['docs/**.md']
  workflow_dispatch:  # <-- add this line

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      
      - name: Install Pandoc
        run: sudo apt-get install pandoc texlive-xetex
        
      - name: Build PDF
        run: pandoc docs/*.md -o ebook.pdf --pdf-engine=xelatex
        
      - name: Build EPUB  
        run: pandoc docs/*.md -o ebook.epub
        
      - name: Deploy to site
        run: |
          mkdir -p _site/ebooks
          mv ebook.* _site/ebooks/
          
      - uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./_site
